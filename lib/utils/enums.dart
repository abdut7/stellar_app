enum EnumLoginView {
  loginFromView,
  chooseOrganizationView,
  emailVerificationView,
  otpView,
  registrationView,
}

enum EnumRemoveType {
  deviceRemove,
  userGroupRemove,
  removeSingleDevice,
  deviceGroupRemove,
  userRemoveFromSharedList
}
